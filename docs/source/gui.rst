Interactive finite-length current profile
-----------------------------------------
In this example the current per unit length is plotted along a cylindrical
probe, allowing the user to interactively change then length and voltage of the
probe. All quantities are normalized.

.. literalinclude:: ../../demo/gui.py
